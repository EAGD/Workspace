#!groovy

def function(n1,n2){
    def suma=0;
    int parseInt(n1,n2);
    suma = n1 + n2;
    echo suma
}

pipeline {
    agent any
    
    stages{
        stage("Function"){
            steps{
                function("${num1}","${num2}")

            }
        }
    }
}